# GitHub Pages 部署流程图

## 🔄 完整工作流程

```
┌─────────────────────────────────────────────────────────────────┐
│                     GitHub Actions 触发                          │
│  • 每小时自动运行 (cron: 0 * * * *)                             │
│  • 代码推送到 main 分支                                          │
│  • 手动触发                                                      │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                   1. Checkout 代码                               │
│  actions/checkout@v4                                             │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                   2. 设置 Go 环境                                │
│  actions/setup-go@v5 (Go 1.24)                                   │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                   3. 构建 HTML 生成器                            │
│  go build -o generate-html ./cmd/generate-html                   │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                   4. 生成 HTML                                   │
│  ./generate-html                                                 │
│  ├─ 获取 Claude release notes                                   │
│  ├─ 获取 Codex release notes                                    │
│  ├─ 获取 Cursor release notes                                   │
│  ├─ 获取 Gemini release notes                                   │
│  ├─ 获取 Qwen release notes                                     │
│  └─ 生成 release-notes.html                                     │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                   5. 准备 GitHub Pages 内容                      │
│  mkdir -p gh-pages                                               │
│  mv release-notes.html gh-pages/index.html                       │
│  touch gh-pages/.nojekyll                                        │
│  echo "Last updated: ..." > gh-pages/last-update.txt             │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                   6. 部署到 gh-pages 分支                        │
│  peaceiris/actions-gh-pages@v3                                   │
│  ├─ 创建/更新 gh-pages 分支                                     │
│  ├─ 推送 gh-pages/ 目录内容                                     │
│  └─ 提交信息: "Update release notes - <sha>"                    │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                   7. GitHub Pages 自动部署                       │
│  GitHub 检测到 gh-pages 分支更新                                │
│  自动构建和发布静态网站                                          │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                   ✅ 网站上线                                    │
│  https://<username>.github.io/<repo>/                            │
└─────────────────────────────────────────────────────────────────┘
```

## 📂 分支结构

```
Repository
│
├── main 分支 (源代码)
│   ├── .github/workflows/release-notes.yml  ← 工作流配置
│   ├── cmd/generate-html/main.go            ← HTML 生成器
│   ├── internal/release_notes/              ← Release notes 逻辑
│   ├── web/templates/                       ← HTML 模板
│   └── ...其他源代码
│
└── gh-pages 分支 (自动生成，仅包含静态文件)
    ├── index.html          ← 生成的 Release Notes 页面
    ├── .nojekyll           ← 禁用 Jekyll 处理
    └── last-update.txt     ← 更新时间戳
```

## 🔐 权限流程

```
┌─────────────────────────────────────────────────────────────────┐
│                   GitHub Actions 权限                            │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│  需要的权限:                                                     │
│  • contents: write  ← 创建/更新 gh-pages 分支                   │
│  • pages: write     ← 部署到 GitHub Pages                       │
│  • id-token: write  ← 身份验证                                  │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│  在仓库设置中配置:                                               │
│  Settings > Actions > General                                    │
│  Workflow permissions: Read and write permissions                │
└─────────────────────────────────────────────────────────────────┘
```

## 🎯 用户配置流程

```
用户操作                          系统响应
─────────                         ─────────

1. 推送代码到 GitHub
   └─> main 分支更新
                                  └─> 触发 GitHub Actions

2. 配置 Actions 权限
   Settings > Actions > General
   └─> Read and write permissions
                                  └─> Actions 获得推送权限

3. 手动触发首次运行
   Actions > Run workflow
                                  └─> 创建 gh-pages 分支
                                  └─> 生成并推送 index.html

4. 配置 GitHub Pages
   Settings > Pages
   └─> Source: gh-pages branch
                                  └─> GitHub 部署网站
                                  └─> 网站上线

5. 访问网站
   https://<username>.github.io/<repo>/
                                  └─> 显示 Release Notes 页面
```

## ⏰ 自动更新时间线

```
时间轴:

00:00 ─┬─> GitHub Actions 触发 (cron)
       │
00:01  ├─> 获取最新 release notes
       │
00:02  ├─> 生成 HTML
       │
00:03  ├─> 推送到 gh-pages 分支
       │
00:04  ├─> GitHub Pages 检测更新
       │
00:05  └─> 网站更新完成 ✅

01:00 ─┬─> 下一次自动更新
       │
       ...

每小时重复
```

## 🔄 数据流

```
外部数据源                    内部处理                    输出
───────────                   ────────                    ────

GitHub API                    
  ├─ Claude releases    ─┐
  ├─ Codex releases     ─┤
  ├─ Cursor releases    ─┼─> Fetchers ─> Service ─> Template ─> HTML
  ├─ Gemini releases    ─┤                                         │
  └─ Qwen releases      ─┘                                         │
                                                                    │
                                                                    ▼
                                                            GitHub Pages
                                                                    │
                                                                    ▼
                                                            用户浏览器
```

## 📊 文件转换流程

```
源文件                          中间文件                    最终文件
──────                          ────────                    ────────

web/templates/                  
release_notes_static.html       
        │                       
        │ (Go template)         
        ▼                       
    Template Engine             
        │                       
        │ + Release Notes Data  
        ▼                       
release-notes.html ─────────> gh-pages/index.html ─────> 网站首页
                    (重命名)                        (部署)
```

## 🎨 页面结构

```
index.html (Release Notes 页面)
│
├── Header
│   ├── 标题: "🚀 CLI Release Notes"
│   └── 更新时间
│
├── Tabs (CLI 选择)
│   ├── Claude CLI
│   ├── Codex CLI
│   ├── Cursor Agent
│   ├── Gemini CLI
│   └── Qwen CLI
│
└── Content (每个 CLI)
    ├── CLI 信息
    │   ├── 显示名称
    │   └── 最新版本
    │
    └── Release 列表
        ├── Release 1
        │   ├── 版本号
        │   ├── 发布日期
        │   ├── Changelog (可展开)
        │   └── GitHub 链接
        │
        ├── Release 2
        └── ...
```

这个流程图清楚地展示了从代码推送到网站上线的完整过程！
