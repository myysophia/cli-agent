# 金融分析脚本环境变量配置示例
# 复制此文件为 .env 并填入实际值（可选）

# cli-agent HTTP 服务地址（必填）
API_URL=http://localhost:8081/chat

# 使用的 AI Profile（可选，默认使用 cursor）
# 可选值: cursor, cursor-gpt5, qwen, kimi, minimax, glm, gemini 等
# 参考 configs/configs.json 中的 profiles 配置
PROFILE=cursor

# 请求超时时间（秒，可选，默认 300）
TIMEOUT=600

# 可选：代理配置
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=http://proxy.example.com:8080
